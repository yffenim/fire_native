import { atom } from "recoil";

// for logic in user flow on FirstTimeScreen
export const secondStatusAtom = atom({
	key: "secondStatusAtom",
	default: ""
});

export const thirdStatusAtom = atom({
	key: "thirdStatusAtom",
	default: ""
});
