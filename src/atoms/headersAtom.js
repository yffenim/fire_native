import { atom } from "recoil";

// get the header from authentication and put it here in atom


// request header
export const headersAtom = atom({
	key: "headersAtom",
	default: {"default": "default"}
});


